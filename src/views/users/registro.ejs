<!DOCTYPE html>
<html lang="en">
  <!-- head INICIO -->
  <%- include("../partials/head.ejs", {title: "Registro"}) %>
  <!-- head FIN -->

  <body>
     <!-- header INICIO -->
     <%- include("../partials/header") %>

     <main>

      <!-- Banner de categorias y miga de pan -->
      <div class="py-6 bg-img-cover bg-dark bg-overlay-gradient-dark position-relative overflow-hidden mb-4 bg-pos-center-center"
      style="background-image: url(/img/header-banner-registro.png);">
          <div class="container position-relative z-index-20" data-aos="fade-right" data-aos-delay="300">
              <nav aria-label="breadcrumb">
                  <ol class="breadcrumb">
                  <li class="breadcrumb-item breadcrumb-light"><a href="/">Home</a></li>
                  <li class="breadcrumb-item active breadcrumb-light" aria-current="page">Usuario</li>
                  <li class="breadcrumb-item active breadcrumb-light" aria-current="page">Registro</li>
                  </ol>
              </nav>                
              <h1 class="fw-bold display-6 mb-4 text-white">Registro</h1>
              <div class="col-12 col-md-6">
                  <p class="lead text-white mb-0">
                      Registrate en nuestro Site
                  </p>
              </div>
          </div>
      </div>
      <!-- Banner de categorias -->

      <section class="mt-5 container ">

        <h1 class="mb-4 display-5 fw-bold text-center">Nuevo Registro</h1>
          <p class="text-center mx-auto">Súmate ya a nuestro ritual</p>
            <div class="border-bottom pb-1"></div>
            

            <!-- Formulario de login-->
            <div class="row d-flex mt-auto p-1">
              <div class="p-5 sticky-md-top top-10">                          

                    <div class="card flex-grow-1 mb-md-0 w-60 mx-auto">
                      <div class="card__content">                         
                           
                          <form class="form-registro" action="/user/registro" method="POST" enctype="multipart/form-data" id="form-registrar">
                          
                          <!--* Nombre -->
                          <label for="nombre" class="form-label">Nombre</label>
                          <input type="text" class="form-control <%= locals.errors && errors.nombre ? 'is-invalid' : null %>" name="nombre" id="nombre" value='<%= locals.oldData ? oldData.nombre : null  %>'>
                          <!--? Validación Backend-->
                          <% if (locals.errors && errors.nombre) { %>                            
                            <p style="color: tomato; font-size: 12px;"> <%= errors.nombre.msg %> </p> 
                          <% } %>
                          <!--?Validación Frontend-->
                          <p id="errorNombre" class="msg-error ocultar"></p>
                          
                          <!--* Apellido -->  
                          <label for="apellido" class="form-label">Apellido</label>
                          <input type="text" class="form-control <%= locals.errors && errors.apellido ? 'is-invalid' : null %>" name="apellido" id="apellido" value='<%= locals.oldData ? oldData.apellido : null  %>' minlength="4">
                          <!--? Validación Backend-->
                          <% if (locals.errors && errors.apellido) { %>                            
                          <p style="color: tomato; font-size: 12px;"> <%= errors.apellido.msg %> </p> 
                          <% } %>
                          <!--?Validación Frontend-->
                          <p id="errorApellido" class="msg-error ocultar"></p>
                          
                          <!--* Email -->
                          <label for="email" class="form-label">Direccion de email</label>
                          <input type="email" class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>" name="email" id="email" value='<%= locals.oldData ? oldData.email : null  %>'>
                          <!--? Validación Backend-->
                          <% if (locals.errors && errors.email) { %>                            
                          <p style="color: tomato; font-size: 12px;"> <%= errors.email.msg %></p> 
                          <% } %>
                          <!--?Validación Frontend-->
                          <p id="errorEmail"class="msg-error ocultar"></p>
                          
                          <!--* Password -->
                          <label for="password" class="form-label">Contraseña</label>
                          <input type="password" class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %>" name="password" id="password" value='<%= locals.oldData ? oldData.password : null  %>' class='form-control spacing <%= locals.errors && errors.password ? 'is-invalid' : null %> '>
                          <!--? Validación Backend-->
                          <% if (locals.errors && errors.password) { %>                            
                            <p style="color: tomato; font-size: 12px;"> <%= errors.password.msg %></p> 
                          <% } %>
                          <!--?Validación Frontend-->
                          <p id="errorPassword" class="msg-error ocultar"></p>
                          
                          <!--* Imagen -->
                          <label for="imagen" class="form-label">Imagen</label>
                          <input type="file" class="form-control" name="imagen" id="imagen">
                          <!--? Validación Backend -->
                          <% if (locals.errors && errors.imagen) { %>                            
                            <p style="color: tomato; font-size: 12px;"> <%= errors.imagen.msg %></p> 
                          <% } %>
                          <!-- Validación Frontend -->
                          <p id="errorImagen" class="msg-error ocultar"></p>

                          <!-- Accept Terms Checkbox-->
                          <div class="form-group form-check my-4">
                            <input type="checkbox" class="form-check-input" id="accept-terms" checked>
                            <label class="form-check-label fw-bolder" for="accept-terms">Acepto los <a href="#">térmimos & condiciones</a></label>
                          </div> 
                          <p>Tus datos personales se utilizarán para procesar tu pedido, mejorar tu experiencia en esta web, gestionar el acceso a tu cuenta y otros propósitos descritos en nuestra política de privacidad.</p>

                          <button class="btn btn-dark mt-4 mb-2" type="submit">Registrar</button>
                        </form>

                      </div>
                    </div>

              </div>                
            </div>
          <!-- /form -->

      </section>

    </main>

     <!--vinculación con archivo .js-->
     
     <script src="/js/RegisterValidation.js"></script>
    
     <!--footer start-->
     <%- include("../partials/footer.ejs") %>
      <!-- main-footer end -->
</body>
</html>

  </body>
</html>
